services:
  grafana:
    user: root
    container_name: 'gcore-stats-datasource-plugin'

    build:
      context: .
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-12.1.1}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
    volumes:
      - ../dist:/var/lib/grafana/plugins/gcore-stats-datasource-plugin
      - ../provisioning:/etc/grafana/provisioning
      - ..:/root/gcore-stats-datasource-plugin
    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.gcore-stats-datasource-plugin:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: gcore-stats-datasource-plugin
